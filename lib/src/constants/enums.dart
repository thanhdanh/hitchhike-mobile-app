enum Vehicle_Type {
  Car,
  Motorcycle,
}

enum Gender { Female, Male, Other }

enum DriverLicenseClass { A1, A2, A3, A4, B1, B2, C, D }

enum RequestMethod { POST, GET, PUT, DELETE }

enum AuthenticationStatus { unknown, authenticated, unauthenticated }
enum AuthenticationType { unknown, google, facebook, phoneNumber }
